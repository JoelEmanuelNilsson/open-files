# Provenance

Everything here is ours to change. Some of it started as someone else's work.

## `extensions/notify.ts`, `extensions/side-chat/`, `extensions/multi-edit.ts`, `skills/mermaid/`

Rewritten from ideas in [`mitsupi`](https://www.npmjs.com/package/mitsupi) v1.6.0,
Apache-2.0, Copyright (c) Mario Zechner. The mermaid skill stays close to the
original (ported to the `@earendil-works/*` package names). `notify.ts`,
`multi-edit.ts` and `side-chat/` (the `/btw` command) are reimplementations
against the current pi API.

## `skills/commit/`

Verbatim from [`mitsuhiko/agent-stuff`](https://github.com/mitsuhiko/agent-stuff/blob/main/skills/commit/SKILL.md),
Copyright (c) Armin Ronacher.

