# Global

## Vendored source

Prefer pinned source over memory or web search; read-only.
Effect v4: `~/.agents/repos/effect`. Alchemy v2: `~/.agents/repos/alchemy`.

## Engineering

Solve the system, not the result — results are datapoints. Fixes
generalize or they don't ship: make the error class impossible by design.
First principles; existing code, docs, and rulings are datapoints, not
truth. Production-grade or no implementation. Complexity must solve a
real, repeated problem — one good solution. Find the true limits from
first principles, declare them, live with them; state only those.

## Process

Tight feedback loops, like game dev: seconds/minutes, not hours.

When a step doesn't need my input, keep going. Put status notes in the
same message as your next action.
Stop and ask only when you can't continue without me, or before anything
destructive: deleting data, force-pushing.
